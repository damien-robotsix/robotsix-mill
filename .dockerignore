# Keep local-only artifacts out of the build context so they can't be
# baked into the image (notably .mill-data, .data).
.git
.venv
venv
.mill-data
# Host's data dir — bind-mounted to /data in compose, so it must NOT be
# baked into /app/.data via the `COPY . /app` step. When it was baked in,
# every restart resurrected stale test pollution and divorced the running
# mill from what the host shows.
.data
.pytest_cache
__pycache__
*.py[cod]
*.egg-info
dist
build

# Documentation (not needed at runtime — the image runs `robotsix-mill`,
# not a doc server).
docs/
mkdocs.yml
AGENT.md
CONTRIBUTING.md
SECURITY.md
LICENSE

# Build & CI configuration (not needed in the image).
Dockerfile
.dockerignore
.github/
.hadolint.yaml
.jscpd.json
.pre-commit-config.yaml
.trivyignore
Makefile

# Example / dev configs (not needed at runtime).
docker-compose.override.example.yml
dev/

# Test coverage artifact (NOT tests/ — the dev stage doubles as the
# sandbox image and needs tests/ for the in-sandbox pytest gate).
.coverage
