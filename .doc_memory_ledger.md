## Runtime worker docstrings (D102 sweep)

- **Target files:** `src/robotsix_mill/runtime/worker/` (8 files)
- **Pyproject.toml change:** Line 260 — removed `"D102"` from the worker glob and the trailing `# FIXME: add method docstrings` comment. Other codes (`C901`, `S110`, `S603`, `S607`, `RUF012`) left intact.
- **Docstrings status:** All 8 files were reviewed. Only one D102 violation remained (`Worker.stop()` in `core.py` line 1120). The `stop()` method had a `#` comment instead of a docstring; needs a Google-style docstring added.
- **Changeling:** `changelog.d/d102.misc.md` created.
- **Layout of worker/ submodules:**
  - `__init__.py` — re-exports, no classes → no public methods to document.
  - `_base.py` — `_WorkerBase` (private class), no public methods.
  - `_gate.py` — `PriorityGate`: all 5 public methods (`locked`, `waiting`, `acquire`, `release`, `slot`) already had docstrings.
  - `core.py` — `Worker`: `queue_size`, `queue_join`, `enqueue`, `requeue_with_current_priority`, `start`, `reconcile_consumers`, `stop`, `requeue_unfinished`. All had docstrings except `stop`.
  - `epic.py` — module-level functions only, no classes.
  - `periodic_passes.py` — `PeriodicPassesMixin`: all methods start with underscore (no public methods).
  - `poll_loops.py` — `PollLoopsMixin`: all methods start with underscore (no public methods). Module-level `_dependabot_title` and `_dependabot_body` also private.
  - `processing.py` — module-level functions only, no classes.