/*
 * robotsix-board — shared board behavior (skeleton placeholder).
 *
 * TODO(build-out): port the shared refresh loop, move-control handling, and
 * detail-panel (#drawer) hydration here. The create step only ships this
 * placeholder so the file exists on disk and resolves via
 * robotsix_board.static_dir(). The real JS hydration lands in the dedicated
 * build-out ticket, after the shared markup/CSS slice. Only the #board
 * columns/cards/move + #drawer/detail-panel + refresh loop are shared;
 * mill-specific chrome stays in robotsix-mill.
 */
