# robotsix_llmio openrouter

OpenRouter transport layer — base model-family-agnostic provider.

## Exports

- `OpenRouterProvider` — base provider that dispatches to OpenRouter API
- `OpenRouterModel` — model that configures OpenRouter-specific request options
