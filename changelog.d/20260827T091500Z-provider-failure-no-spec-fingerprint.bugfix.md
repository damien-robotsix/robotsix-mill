Implement no longer records a spec fingerprint when the agent failed for a provider/model reason (invalid model id, token cap hit before any output, output retries exhausted) — such tickets blocked, then re-blocked with "spec unchanged" on every resume even after the model configuration was fixed. The dual primary+fallback failure now also keeps both exceptions typed so the stage can classify it.
