Close the check-then-create race in `POST /tickets/ingest`. The dedup pass listed candidates, ran a token prefilter and an LLM check, then created — a window that spans minutes under load. Concurrent identical reports all read a board without the ticket, all missed the title fingerprint, and all created. The final create now re-runs the fingerprint check against freshly-listed candidates while holding a per-board lock, so concurrent identical ingests resolve to one ticket plus N-1 dedup hits.
