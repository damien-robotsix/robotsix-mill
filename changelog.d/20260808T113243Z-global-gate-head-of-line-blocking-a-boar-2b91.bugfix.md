Fix head-of-line blocking at the global concurrency gate: sort the boot drip's enqueue order by (priority_rank, stage_rank) so priority tickets arrive in early batches, and add a drain-at-gate-entry loop so consumers swap to a better-ranked ticket that arrived between pop and gate admission. Prevents a board's consumer from parking at the global gate with a stale low-ranked ticket while a priority/near-terminal ticket sits unseen in its queue (max_concurrency=1 per board).
