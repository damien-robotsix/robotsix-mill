The implement agent's system prompt shrank from ~6 700 to ~1 400 words: the mandatory 3-cycle ruff/mypy/deptry/vulture pre-flight loop (the stage gate and CI own that), the contradictory verify-every-edit vs verify-once guidance, and the incident changelog of one-off rules are gone. Python-specific operational rules (`uv run` prefix, PEP 758, vulture decorators, lint-once) moved to `language_instructions/python.md` (implement now sets `inject_language_conventions: true`); mill-specific rules (modules.yaml registration, changelog-fragment registration, `resolve_docker_digest.py`, experts) moved to this repo's `AGENT.md`, which is injected per repo.
