Documented `POST /tickets/{id}/reset-fingerprint` in the chat skill. The endpoint has existed since #2357 but was invisible to the chat agent, which left clearing a stale-spec guard as an error-prone manual step — and the deletion rule actively pointed at `DELETE` for exactly the fingerprint-guarded tickets `reset-fingerprint` is for. That rule now points at `reset-fingerprint` + `resume-blocked` instead.
