Stop blocking tickets on four recoverable conditions that needed no human: GitHub throttle responses (403/429 carrying a rate-limit signal) are now classified transient and retried instead of blocking the ticket; a PR-create 422 whose existing-PR lookup was itself refused surfaces the refusal rather than a misleading "PR create failed"; a changelog fragment whose kind has no home in the repo falls back to the repo's generic bucket instead of raising; and a merge conflict found during ci_fix is handed to the rebase agent, matching what the merge stage already does from its own parked states.
