The agent's `run_command` now refuses a pytest invocation that would run the WHOLE suite (bare `pytest`, `pytest tests/`, `python -m pytest .`, `uv run pytest -q`) and points the agent at targeted runs; the stage-owned gate runs the full suite once the agent stops. Live (7d to 2026-08-29) in-loop pytest was 2,302 runs / 91 h, single traces ran `pytest tests/` at 231 s, 509 s and 927 s, and every run over ~5 min expired the provider prompt cache so the next turn re-sent the whole context uncached. Toggle: `run_command_refuse_full_suite`.
