A failed triage classifier no longer parks the raw draft at `HUMAN_ISSUE_APPROVAL` ("triage classifier failed (model unavailable) — routing to human approval" was 26 of 76 gate entries in the week to 2026-08-29); the stage falls through to the full refine pass. A model outage inside the post-refine auto-approve classifier now propagates so the worker parks the ticket instead of the human.
