Rebase drop-guard now distinguishes sibling-PR supersession from genuine drops. When the target branch changes a file during the rebase window (a sibling PR landed), the file is reported as "sibling-likely" with a targeted BLOCKED message rather than a generic "silently dropped" message. This prevents false-positive drop-guard blocks when a sibling PR's changes correctly supersede the branch's delta.
