New deterministic periodic pass `blocked-auto-resume`: a BLOCKED ticket whose latest block note is resumable (`agent error — resumable`, stage timeout, `clone missing`, `pr_urls.json corrupted`, ci_fix iteration budget exhausted, tests still failing, review rounds exhausted, model outage) is resumed automatically once it has been blocked for `blocked_auto_resume_cooldown_seconds` (1800), at most `blocked_auto_resume_max_per_ticket` (1) times, leaving an `[auto-resume n/m]` comment. Spec-fingerprint blocks and upstream-CI parks are never touched. Week to 2026-08-29: 77 blocks, 31 resumed by hand with exactly this playbook.
