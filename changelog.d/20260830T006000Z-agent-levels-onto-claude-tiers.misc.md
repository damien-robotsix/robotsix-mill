Agent YAML default levels moved off the keyed (paid) OpenRouter tiers onto the Claude subscription tiers, per operator preference: every level-1 classifier/gate (auto-approve, dedup, standards_gate, spec-review, scope/ops classify, reviewer-agreement, test_scope, and the level-1 periodic scanners) → level 2 (haiku); review, rebase, review_revision, answer and the level-3 periodic analysts → level 4 (opus); triage, codeql_fp_triage, run_tests → level 2. `trace_review_model_level` default 1 → 2 and `refine_trivial_model_level` 3 → 4 match production. implement/ci_fix stay at YAML level 3 (production pins them to 4 via `agent_levels`).
