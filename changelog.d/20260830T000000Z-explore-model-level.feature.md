Run the exploration sub-agent on a configurable tier (`explore_model_level`, default 2 = haiku on the Claude subscription) instead of hard-wiring it to paid level-1 flash, whose ~23 s median generations tripped the 30 s explore timeout constantly. Claude tiers build the scout through the llmio provider so it keeps its read-only tools (with the SDK's built-in tools denied) and shares the Claude concurrency bound; a usage-exhaustion error surfaces to the caller like any other explore failure — no paid fallback.
